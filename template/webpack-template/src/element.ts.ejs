<% if(ProjectType==='mobile') {%>import { Badge } from 'vant';

<% } %>import { App } from 'vue';

const element = {
	install: (app: App<Element>) => {<%
    if(ProjectType==='mobile') {%>
		app.component('MuiBadge', Badge);<% } %>
	},
};

export default element;
